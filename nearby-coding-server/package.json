{
  "name": "nearby-coding-server",
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "build": "echo 'No build step required'",
    "start": "echo 'Edge functions do not require a start script'",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  }
}